export type BaseVCSBranchConfig = {
    /**
     * The maximum number of retries for network operations. Only recoverable
     * errors will trigger a retry, such as network and server errors.
     *
     * @default 10
     */
    maxRetries?: number;
};
export type EnvironmentLayout = {
    /**
     * Each entry of this array defines a correspondence between a QA Wolf environment
     * and a VCS branch. Use environment aliases to refer to a particular environment.
     */
    baseEnvironmentsMapping: Array<{
        environmentAlias: string;
        vcsBranch: string;
    }>;
    /**
     * A unique alias for the environment being tested. We recommend using either
     * `pullRequestDetailsToEnvironmentAlias` or the more generic
     * `arbitraryStringToEnvironmentAlias` to generate this value.
     */
    headEnvironmentAlias: string;
};
export type VCSLayout = {
    /**
     * The base VCS branch from which this
     * branch was created. This will allow
     * QA Wolf to determine the base environment
     * to use given `baseEnvironmentsMapping`.
     */
    baseVcsBranch: string;
    /**
     * The VCS branch to test.
     */
    headVcsBranch: string;
};
export type PullRequestTestingLayout = EnvironmentLayout & VCSLayout;
//# sourceMappingURL=types.d.ts.map